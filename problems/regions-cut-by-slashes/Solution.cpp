#include <iostream>
#include <vector>

using namespace std;

class Solution {
public:
    // My approach is to enumerate through the grid to find forward and backward slashes and use dfs for
    // each encountered slash to find disjoint sets (2 boundary cells or cells/slashes that forms a loop)
    // while keeping track of visited cells.
    int regionsBySlashes(vector<string>& grid) {
    }
};
